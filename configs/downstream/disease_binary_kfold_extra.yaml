base_config: disease_binary_base.yaml

experiment:
  name: downstream_disease_binary_kfold_extra_seed42
  output_root: outputs/downstream_disease_binary_kfold_extra_seed42

downstream:
  diseases: ["AD", "MCI", "偏头痛", "癫痫"]
  split_dir: splits/downstream_disease_binary_kfold_extra_seed42/fold_0

downstream_split:
  strategy: subject_stratified_kfold
  out_dir: splits/downstream_disease_binary_kfold_extra_seed42
  diseases: ["AD", "MCI", "偏头痛", "癫痫"]
  num_folds: 5
  seed: 42
  group_by_base_subject_id: true
